<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Search Results</title>
</head>
<body>
    <h1>Search Results for "{{ keyword }}"</h1>
    <a href="/search">← Back to Search</a>

    {% if results | length == 0 %}
        <p>No results found.</p>
    {% else %}
        <ul>
        {% for result in results %}
            <li><a href="{{ result.1 }}" target="_blank">{{ result.0 }}</a></li>
        {% endfor %}
        </ul>
    {% endif %}
</body>
</html>
